// Este archivo es un stub. Lock screen no se usa en el panel web.
import 'package:flutter/material.dart';

class LockScreenPage extends StatelessWidget {
  const LockScreenPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Lock Screen - No disponible en web')));
  }
}

