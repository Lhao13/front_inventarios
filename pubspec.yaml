name: front_inventarios
description: "App de inventarios para el proyecto integrador"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  geolocator: ^14.0.2
  mobile_scanner: ^7.2.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  sqflite: ^2.4.2
  path: ^1.9.1
  connectivity_plus: ^7.0.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
